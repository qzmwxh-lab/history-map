{
  "name": "history-map",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive history map and VR panorama viewer",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
